# Text Hygiene Build Plan

## Gate

- [DECISION] Feature-local requirements: `.godpowers/features/text-hygiene/PRD.mdx`.
- [DECISION] Feature-local architecture: `.godpowers/features/text-hygiene/ARCH-DELTA.mdx`.
- [DECISION] This small feature does not require project-wide roadmap or stack artifacts.
- [DECISION] Baseline hash of the pre-existing `git diff -- vendor` output: `1c40795aed0bbcdfd64a822fee3d05d2683af6224ce84601ddf98309e219e536`.

## Slice TH-01: Deterministic text hygiene inside the one-pass contract

Requirements: P-MUST-01, P-MUST-02, P-MUST-03, P-MUST-04, P-MUST-05, P-MUST-06, P-MUST-07, P-MUST-08, P-MUST-09, P-SHOULD-01.

### Ownership

One executor owns only these feature files:

- `scripts/text_hygiene.py`
- `tests/test_text_hygiene.py`
- `SKILL.md`
- `AGENTS.md`
- `GEMINI.md`
- `.cursor/rules/voiceprint.mdc`
- `.github/copilot-instructions.md`
- `evals/evals.json`
- `README.md`
- `CHANGELOG.md`
- `.github/workflows/vendor-sync-check.yml`

The executor must preserve every pre-existing change, including URL updates in `README.md`, `CHANGELOG.md`, sync scripts, and `vendor/`. The executor must not edit any file under `vendor/` or either sync script.

### Tests first: RED

1. Add `tests/test_text_hygiene.py` using `unittest` only.
2. Add exact fixtures for removable controls, normalized Unicode spaces, stable offsets and counts, tabs and line breaks, contextually preserved complex-script joiners, valid emoji sequences represented with Unicode escapes, visible fullwidth and Cyrillic letters, standard input, file input, JSON output, exit codes, and invalid UTF-8.
3. Run `python3 -m unittest discover -s tests -v` and record the expected failure because `scripts/text_hygiene.py` does not yet exist.

### Implementation: GREEN

1. Add `scripts/text_hygiene.py` with an importable inspect and clean API plus `inspect` and `clean` CLI subcommands.
2. Use Python standard-library modules only, O(n) traversal, no network, no telemetry, no in-place write, and no confusable or NFKC conversion.
3. Make `inspect` return exit 1 for actionable findings, 0 for none, and 2 for usage or processing errors. Make successful `clean` return 0.
4. Run the unit suite until it passes.

### Contract integration

1. Update `SKILL.md` so before diagnosis reads the immutable original, cleanup runs once at the start of Step 2, the cleaned working copy reaches one humanizer invocation, and residual diagnosis remains report-only.
2. Keep the six top-level sections. Add deterministic hygiene counts and preservation reasons inside `What changed`.
3. Update every supported adapter to route through the root hygiene contract without duplicating policy tables.
4. Add at least four behavior cases to `evals/evals.json`: positive cleanup, zero findings, preserved load-bearing Unicode, and a detector-framed request that does not expand scope or trigger another rewrite.
5. Update `README.md` and `CHANGELOG.md` with scope, attribution to the linked inspiration, limitations, and the no-detector-gaming boundary.
6. Update CI to run the standard-library unit suite and validate that feature-authored files contain no em dash or en dash characters.

### Refactor and verification

1. Run `python3 -m unittest discover -s tests -v`.
2. Run `python3 -m py_compile scripts/text_hygiene.py tests/test_text_hygiene.py`.
3. Run `python3 -c "import json; json.load(open('evals/evals.json'))"`.
4. Run `sh -n scripts/sync-upstream scripts/check-vendor-headers scripts/check-upstream-freshness`.
5. Run `sh scripts/check-vendor-headers`.
6. Run the hygiene helper against representative positive and zero-finding standard-input samples.
7. Recompute `git diff -- vendor | shasum -a 256` and require the baseline hash above.
8. Search all feature-authored files for em dash, en dash, and accidental emoji literals. Fix any finding.
9. Review the final diff for exactly one cleanup stage, one humanizer rewrite, one residual diagnosis, preserved user URL edits, and no unrelated changes.

### Completion evidence

- All unit and syntax checks pass.
- `evals/evals.json` parses and contains the four new behavior cases.
- The six-section contract remains explicit in `SKILL.md` and every adapter.
- Hygiene reporting is provenance-neutral and appears only in `What changed`.
- The vendor diff hash remains unchanged from the baseline.

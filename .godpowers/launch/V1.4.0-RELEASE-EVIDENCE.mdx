# Voiceprint v1.4.0 Launch and Release Evidence

Prepared: 2026-08-15T21:23:56-04:00
Scope: Local release readiness and GitHub release preparation for the deterministic text-hygiene feature.

## Launch decision

- [DECISION] Release version: `v1.4.0`.
- [DECISION] SemVer rationale: the feature adds backward-compatible behavior and a new standard-library Python helper without removing or changing a public entry point, so a minor version follows `v1.3.0`.
- [DECISION] Product form: local prompt skill plus an offline Python 3.10+ helper. There is no UI, website, hosted service, deploy target, signup flow, operated telemetry, analytics dashboard, OG card, staging URL, or production URL.
- [DECISION] Launch channel: GitHub Releases only. Landing-page copy, social cards, UTM links, signup attribution, Product Hunt, Show HN, Reddit, X, LinkedIn, Indie Hackers, and dev.to are not applicable to this scoped release.
- [DECISION] Existing feature delivery is merged and pushed. Local `HEAD` and `origin/main` both resolved to merge commit `ab0e448fe3154438b14610a5b026e89f40db0c7b` when this evidence was prepared.
- [DECISION] GitHub reported `v1.3.0` as the latest published release on 2026-08-15. It was published on 2026-05-30.

## Gate evidence

| Gate | Evidence | Result |
|---|---|---|
| Hardening | `.godpowers/features/text-hygiene/HARDEN-FINDINGS.mdx` reports 0 open Critical, High, Medium, and Low findings and a passed launch gate. | Pass |
| Release requirements | `.godpowers/REQUIREMENTS.mdx` reports all 9 P-MUST requirements done and P-SHOULD-01 done. The only not-started item is post-release P-COULD-01. | Pass |
| Deploy | `.godpowers/state.json` marks deploy `not-required`, local-readiness scope, with no service or deployment target. | Not applicable |
| Observe | `.godpowers/state.json` marks observe `not-required`, local-readiness scope, with no telemetry, SLO, alert, or runbook surface. | Not applicable |
| External access | No waiting-for-access artifact exists. GitHub release metadata was readable through the configured GitHub CLI. Do not request credentials unless a named publication command fails for authorization. | Pass |
| Public activation | `.godpowers/launch/PREPUBLICATION.mdx` records a hash-bound pass after the release metadata edits and final smoke run. | Pass |

## Local smoke evidence

All commands ran from the repository root on Python 3.14.2.

| Command | Result |
|---|---|
| `python3 -m unittest discover -s tests -v` | Pass: 37 tests discovered, 36 passed, and the opt-in benchmark skipped as designed. |
| `python3 -m py_compile scripts/text_hygiene.py tests/test_text_hygiene.py` | Pass. |
| `python3 -c "import json; json.load(open('evals/evals.json', encoding='utf-8'))"` | Pass. |
| `scripts/check-vendor-headers` | Final pass: every vendored file carries a valid sync header. An ignored `vendor/.DS_Store` caused the first local run to fail and was removed before the successful rerun. |
| `VOICEPRINT_RELEASE_BENCHMARK=1 python3 -m unittest -v tests.test_text_hygiene.TextHygieneApiTests.test_P_MUST_02_p95_is_within_budget_for_one_hundred_thousand_code_points` | Pass: final release-candidate p95 74.7 ms against the 200 ms limit. |
| `printf 'alpha\u200Bbeta\u00A0gamma\n' \| python3 scripts/text_hygiene.py clean --stats` | Pass: stdout was `alphabeta gamma`; manifest reported U+200B removed and U+00A0 normalized at stable offsets. |
| `git diff --check --` | Pass. |
| `git status --short` after the checks | Clean before this evidence artifact was added. |

The release owner must rerun the same checks after the version and changelog commit. The final `git status --short` must be empty after the release work is committed.

Required final smoke commands:

```sh
python3 -m unittest discover -s tests -v
python3 -m py_compile scripts/text_hygiene.py tests/test_text_hygiene.py
python3 -c "import json; json.load(open('evals/evals.json', encoding='utf-8'))"
scripts/check-vendor-headers
VOICEPRINT_RELEASE_BENCHMARK=1 python3 -m unittest -v tests.test_text_hygiene.TextHygieneApiTests.test_P_MUST_02_p95_is_within_budget_for_one_hundred_thousand_code_points
printf 'alpha\u200Bbeta\u00A0gamma\n' | python3 scripts/text_hygiene.py clean --stats
git diff --check --
git status --short
```

## Exact documentation and version changes

No new feature explanation is required. `README.md`, `SKILL.md`, the adapters, and `CHANGELOG.md` already document deterministic text hygiene, preserved `Before` bytes, the one-pass rule, conservative Unicode handling, offline behavior, CLI usage, the performance gate, and the no-provenance boundary.

Apply only these release edits:

1. In `README.md`, change the version badge from `version-1.3.0-blue` to `version-1.4.0-blue`.
2. In `SKILL.md` frontmatter, change `metadata.version` from `1.3.0` to `1.4.0`.
3. In `CHANGELOG.md`, retain an empty `## [Unreleased]` heading and move the existing Added and Changed sections under `## [1.4.0] - 2026-08-15`.
4. Add this Security section to the `1.4.0` entry:

```markdown
### Security

- Restricted path input to unchanged regular, non-symbolic-link files using
  pre-open and post-open identity checks, no-follow behavior where available,
  and nonblocking open behavior where available. Symlinks, FIFOs, and
  check-then-open replacements fail without exposing input or blocking.
- Narrowed variation-selector and emoji-joiner preservation claims to the
  evidence the pinned policy actually establishes. The validation workflow
  now uses read-only repository permissions and immutable action revisions.
```

Recommended release commit subject:

```text
Release v1.4.0: deterministic text hygiene
```

## Release sequence

1. Apply the three documentation and version edits on top of current `main`.
2. Run every command in Local smoke evidence and confirm the expected results.
3. Commit `README.md`, `CHANGELOG.md`, `SKILL.md`, and this evidence artifact with the recommended subject. The text-hygiene feature itself is already merged by commit `ab0e448`.
4. Re-read the authoritative hardening artifact and `.godpowers/state.json` hardening state. Confirm the hash, update time, and open Critical count have not changed adversely.
5. Immediately before the first external public write, run:

```sh
node /Users/hprincivil/.codex/godpowers-runtime/lib/prepublication-gate.js --record --project=.
node /Users/hprincivil/.codex/godpowers-runtime/lib/prepublication-gate.js --check --project=.
```

6. Continue only when the second command prints `verdict: pass`. Push the release commit to `origin/main` only after that pass. If a review branch is required, rerun `--check` immediately before each public branch, pull request, or merge write.
7. Create an annotated local tag on the pushed release commit:

```sh
git tag -a v1.4.0 -m "voiceprint v1.4.0"
```

8. Re-run the prepublication `--check` immediately before pushing the tag. Refuse publication if the record is stale or the verdict is not `pass`.
9. Push the tag, then rerun the prepublication `--check` immediately before creating the public GitHub release.
10. Create a non-draft, non-prerelease GitHub release with the title and body below. Capture the URL emitted by GitHub as the authoritative release URL. Do not predict a release URL before creation.
11. Verify publication with `gh release view v1.4.0 --json tagName,name,isDraft,isPrerelease,publishedAt,url` and confirm the tag resolves to the intended release commit.
12. Run a clean consumer smoke check from the published `v1.4.0` tag, then confirm `git status --short` is empty and local `main` equals `origin/main`.

## GitHub release title

```text
voiceprint v1.4.0
```

## GitHub release body

```markdown
Voiceprint now cleans copy-and-paste Unicode artifacts before its one
humanizer rewrite while keeping the submitted `Before` text byte-for-byte
intact.

### Added

- A dependency-free, offline text-hygiene helper with importable inspection
  and cleanup APIs plus `inspect` and `clean` CLI operations.
- Deterministic manifests for removed, normalized, and deliberately preserved
  Unicode characters, including code points, names, counts, actions, offsets,
  and preservation reasons.
- Exact fixtures for hidden controls, Unicode spaces, multilingual joiners,
  variation selectors, emoji ZWJ pairs, file safety, size limits, and CLI
  behavior.

### Preserved contracts

- `Before` remains byte-for-byte identical to the submitted text and is still
  diagnosed before cleanup.
- Voiceprint still performs exactly one hygiene cleanup, one humanizer
  invocation, and one report-only residual diagnosis. The residual never
  drives another rewrite.
- The result still has six top-level sections. Hygiene details remain inside
  `What changed`.
- Valid script and emoji sequences, fullwidth letters, confusable visible
  letters, tabs, and line breaks remain protected by the conservative policy.

### Safety and compatibility

- Python 3.10 or newer is required for the local helper. No package, service,
  network connection, or runtime dependency was added.
- Input is limited to 4 MiB. File paths must resolve to unchanged regular,
  non-symbolic-link files. The helper never rewrites a source file in place.
- Hygiene reports observable Unicode actions only. It does not claim
  watermark discovery, provenance attribution, detector-signal removal, or a
  promise about any external detector.

### Verification

- 36 routine tests passed, with the opt-in release benchmark separated from
  ordinary CI as designed.
- The 100,000-code-point release benchmark passed at p95 74.7 ms against the
  200 ms limit on the release-preparation machine.
- Independent hardening completed with zero open Critical, High, Medium, or
  Low findings.

No migration is required. Existing Voiceprint invocations keep the same
entry point and six-section result contract.

**Full diff**: https://github.com/hannsxpeter/voiceprint/compare/v1.3.0...v1.4.0
```

## Success criteria

The release is successful when all of the following are true:

- The public GitHub release is non-draft, not a prerelease, and its tag points to the intended release commit.
- A clean checkout of `v1.4.0` passes the routine test suite, syntax check, eval JSON parse, vendor-header check, and focused standard-input smoke.
- The published skill metadata and README badge both report `1.4.0`.
- The changelog contains the dated `1.4.0` entry and retains an `Unreleased` heading.
- No regression changes `Before`, adds another transformation or diagnosis pass, introduces a runtime dependency or network call, or edits a user source file in place.
- No release-blocking issue is reported during D+1 through D+7 follow-up.

## Rollback

Rollback triggers:

- Any submitted `Before` byte changes.
- More or fewer than one before diagnosis, one cleanup, one humanizer invocation, or one residual diagnosis.
- Any source-file overwrite, path-boundary disclosure, blocking special-file read, network call, or unbounded input behavior.
- Any deterministic fixture mismatch or release benchmark above 200 ms p95 on the designated reference runner.

User rollback is immediate and data-free: pin or check out `v1.3.0`. There is no schema, service, state migration, or stored data to reverse.

Maintainer rollback after publication:

1. Keep the immutable `v1.4.0` tag and release available as historical evidence. Do not retarget the tag.
2. Mark the GitHub release description with a concise known-issue notice if users must stop adopting it.
3. Create a hotfix branch from current `main` and revert merge commit `ab0e448` with mainline parent 1.
4. Update public version references and the changelog for a `v1.4.1` rollback release.
5. Run the full release smoke set and hardening gate again, then create a fresh hash-bound prepublication record before publishing `v1.4.1`.
6. Tell affected users to pin `v1.3.0` until `v1.4.1` is available.

## D-1 through D+7 runbook

| Date | Owner | Action |
|---|---|---|
| D-1, 2026-08-15 | Voiceprint maintainer | Apply release metadata edits, run all local checks, commit, confirm hardening and P-MUST gates, and record a fresh prepublication pass immediately before the first public write. |
| D-Day, 2026-08-15 | Voiceprint maintainer | Push the release commit and tag, publish the GitHub release, capture its emitted URL, verify release metadata, and run the clean-tag consumer smoke. |
| D+1, 2026-08-16 | Voiceprint maintainer | Review GitHub Actions and new issues for installation, Unicode preservation, one-pass, path safety, or Python compatibility regressions. Respond to every release-related report. |
| D+3, 2026-08-18 | Voiceprint maintainer | Repeat the clean-tag smoke on a fresh checkout and review all feedback. Invoke rollback if a trigger is met. |
| D+7, 2026-08-22 | Voiceprint maintainer | Record a short retrospective against the success criteria, including failures, fixes, user reports, and whether P-COULD-01 has gained any real host-integration demand. |

## State handoff

The owning Godpowers command can register this evidence with these structured values after the release metadata commit and final smoke rerun:

```yaml
status: pending
artifact: launch/V1.4.0-RELEASE-EVIDENCE.mdx
readiness-scope: local-readiness
verified-target-type: local
release-version: v1.4.0
release-channel: github-releases
publication-state: not-published
prepublication-verdict: pending
hardening-open-critical: 0
must-requirements-done: 9
local-checks: passed
external-access-waiting: false
```

Change `status` to `done`, `publication-state` to `published`, and `prepublication-verdict` to `pass` only after the hash-bound gate passes, GitHub publishes the release, and the clean-tag consumer smoke succeeds. The orchestrator owns the state mutation and generated launch view refresh.

## Prepublication readiness verdict

The hardening and requirement gates pass, the release-candidate smoke set passes, and no deploy, observability, URL, OG-card, analytics, or provider access is required. A fresh hash-bound record and check returned `verdict: pass` after the documentation and version edits. The candidate is approved for the GitHub publication sequence, provided `--check` is repeated immediately before each release write.

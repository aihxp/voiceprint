# Initialization handoff

## User request

Add the useful capabilities offered by `https://github.com/guillaumemeyer/watermarks-remover` to the existing `voiceprint` skill.

## Detected mode and scale

- Existing brownfield skill repository.
- Small feature addition.
- No frontend or runtime service.

## Existing product constraints

- `voiceprint` is currently a thin prompt-only orchestrator over two vendored skills: `authenticity-check` and `humanizer`.
- Its defining contract is one diagnosis, one rewrite, one residual diagnosis, with no optimization loop.
- Its documented scope rejects detector-gaming and claims about defeating AI detectors.
- Files under `vendor/` are synced copies and must not be edited by hand.
- Repository instructions forbid em dash and en dash characters and forbid emojis in all new output.

## Linked project findings

- The upstream is MIT licensed and currently presents release 0.4.0.
- It offers deterministic Unicode cleanup, statistical watermark rewrite hooks, provenance and metadata cleanup for several file types, optional external image scoring, and optional external pixel regeneration.
- The deterministic Unicode hygiene is compatible with prose quality and privacy when reported honestly.
- Statistical detector-evasion rewriting conflicts with voiceprint's no-detector-gaming scope and duplicates its single humanizer rewrite.
- Broad file metadata and pixel removal would be a new product surface rather than a natural part of the prose pass.

## Working tree safety

- The working tree was already dirty before this request.
- Existing changes update upstream owner URLs from `aihxp` to `hannsxpeter`, including synchronized header lines under `vendor/`.
- Preserve all pre-existing changes and do not revert or overwrite them.

## Pillars status

- No `.godpowers/` state or `agents/` pillar files existed before this fallback initialization.
- No UI surface was detected.

## Routing note

Initialize only the minimal Godpowers and Pillars state needed by the `god-feature` workflow. Do not start a full autonomous project build. Return control so the feature workflow can plan a compatibility-preserving integration.
